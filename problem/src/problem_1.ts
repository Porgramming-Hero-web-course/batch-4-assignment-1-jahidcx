{
    // problem-01

    const sumArray = (numberArray: number[]): number => {
        const sumOfArray = numberArray.reduce((acc, value) => acc + value, 0);
        return sumOfArray; 
    }

    // const sum = sumArray([1, 2, 3, 4, 5,10]);
    // console.log(sum);

























}